name = "ib_tws_api"
version = "0.1.0"

description = "A Gleam wrapper for Interactive Brokers TWS/Gateway API"
licences = ["Apache-2.0"]
repository = { type = "github", user = "", repo = "" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"

[targets.erlang]
native-files = ["native/src/socket_options.erl", "native/src/socket_helper.erl"]
